services:
  - type: web
    name: uncle-roy-chess-assistant
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
    plan: free
    autoDeploy: true
